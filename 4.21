/*Даны вещественные числа а, b, с (а ≠ 0). Выяснить, имеет ли уравнение
ах 2 + bх + с = 0 вещественные корни. Найдите эти корни.*/

#include <iostream>
#include <math.h>
using namespace std;

int main() {

int a, b, c, x1, x2, D;

cin >> a;
cin >> b;
cin >> c;

D = pow(b, 2)-4*a*c;

if (D >= 0) {
  x1 = (-b- sqrt(D))/(2*a);
  x2 = (-b+ sqrt(D))/(2*a);
  cout << x1 << "  " << x2 <<endl;
}

else {
  cout << "Net kornej" << endl;
}


}
